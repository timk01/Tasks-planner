services:
  task-planner:
    build: .
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "5173:80"

  summarization-service:
    build: ${SUMMARIZATION_BUILD_CONTEXT}
    env_file:
      - ${SUMMARIZATION_BUILD_CONTEXT}/.env
    depends_on:
      kafka:
        condition: service_healthy

  scheduler:
    build: ${SCHEDULER_BUILD_CONTEXT}
    env_file:
      - ${SCHEDULER_BUILD_CONTEXT}/.env
    depends_on:
      kafka:
        condition: service_healthy

  email-sender:
    build: ${EMAIL_SENDER_BUILD_CONTEXT}
    env_file:
      - ${EMAIL_SENDER_BUILD_CONTEXT}/.env
    depends_on:
      kafka:
        condition: service_healthy

  db:
    image: ${POSTGRES_IMAGE}
    container_name: tasks_planner_postgres_compose
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - tasks_planner_postgres_data:/var/lib/postgresql/data

  kafka:
    image: ${KAFKA_IMAGE}
    container_name: kafka_compose
    ports:
      - "9094:9094"
    environment:
      KAFKA_NODE_ID: ${KAFKA_NODE_ID}
      KAFKA_PROCESS_ROLES: ${KAFKA_PROCESS_ROLES}
      KAFKA_LISTENERS: ${KAFKA_LISTENERS}
      KAFKA_ADVERTISED_LISTENERS: ${KAFKA_ADVERTISED_LISTENERS}
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: ${KAFKA_LISTENER_SECURITY_PROTOCOL_MAP}
      KAFKA_INTER_BROKER_LISTENER_NAME: ${KAFKA_INTER_BROKER_LISTENER_NAME}
      KAFKA_CONTROLLER_QUORUM_VOTERS: ${KAFKA_CONTROLLER_QUORUM_VOTERS}
      CLUSTER_ID: ${CLUSTER_ID}
      KAFKA_CONTROLLER_LISTENER_NAMES: ${KAFKA_CONTROLLER_LISTENER_NAMES}
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: ${KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR}
    volumes:
      - tasks_planner_kafka_data:/tmp/kraft-combined-logs
    healthcheck:
      test: ["CMD-SHELL", "/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server localhost:9092 > /dev/null 2>&1"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

volumes:
  tasks_planner_postgres_data:
  tasks_planner_kafka_data: